"""Telemetrees API — continuous dependency monitoring.

Deliberately empty of re-exports. `contracts.py` is the only module anything outside this
package imports from (`docs/PRINCIPLES.md` §1.1).

This API surfaces "this exists now"; it never decides whether to adopt it. That call belongs
to a human, with Proving Grounds doing the actual testing (§1).
"""
