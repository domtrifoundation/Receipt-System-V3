"""Data-subject-rights requests — export (portability) and deletion (erasure).

Deliberately empty of re-exports, same reasoning as `core/audit/__init__.py`:
`contracts.py` is the only module anything outside this package imports from
(`docs/PRINCIPLES.md` §1.1).
"""
